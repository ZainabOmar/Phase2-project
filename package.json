{
  "name": "Articles",
  "version": "1.0.0",
  "author": "",
  "description": "Your new favorite article",
  "scripts": {
    "start": "babel -w . --out-dir compiled --presets=es2015,react --ignore=node_modules,compiled --source-maps inline | live-server --open=./index.html --port=8081",
    "build": "babel -w . --out-dir compiled --presets=es2015,react --ignore=node_modules,compiled --source-maps inline | live-server --open=./index.html --port=8081",
    "build:watch": "babel -w . --out-dir compiled --presets=es2015,react --ignore=node_modules,compiled --source-maps inline | live-server --open=./index.html --port=8081",
    "test": "live-server --open=./SpecRunner.html --port=8081",
    "postinstall": "curl -s https://raw.githubusercontent.com/reactorcore/pomander/master/bin/install | bash"
  },
  "license": "UNLICENSED",
  "private": false,
  "dependencies": {
    "bootstrap": "^4.0.0-alpha.2",
    "consolidate": "^0.14.5",
    "express": "^4.14.1",
    "jquery": "^2.2.1",
    "lodash": "^4.6.1",
    "mongodb": "^2.2.22",
    "mustache": "^2.3.0",
    "react": "^0.14.7",
    "react-dom": "^0.14.7",
    "requirejs": "^2.3.2"
  },
  "devDependencies": {
    "babel-preset-react": "^6.22.0",
    "eslint": "^3.15.0",
    "eslint-config-google": "^0.7.1"
  },
  "main": "articles.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ZainabOmar/project.git"
  },
  "keywords": [],
  "bugs": {
    "url": "https://github.com/ZainabOmar/project/issues"
  },
  "homepage": "https://github.com/ZainabOmar/project#readme"
}
